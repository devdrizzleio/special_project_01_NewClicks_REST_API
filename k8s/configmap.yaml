apiVersion: v1
kind: ConfigMap
metadata:
  name: newclicks-config

data:
  PORT: "5000"
  MONGO_URI: "mongodb://mongo:27017/newclicks"
  BASE_URL: "http://localhost"
  DEFAULT_COMMISSION_RATE: "0.10"
  TRACKING_CODE_LENGTH: "12"